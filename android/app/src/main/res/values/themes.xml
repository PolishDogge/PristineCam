<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Minimal XML theme used only during Activity launch (splash window).
        The full Material3 theme is applied in Compose via PristineCamTheme.
    -->
    <style name="Theme.PristineCam" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
